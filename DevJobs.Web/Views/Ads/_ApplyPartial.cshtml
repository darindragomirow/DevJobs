@using DevJobs.Web.Models
@model AdViewModel
<div>
    @using (Html.BeginForm("Apply", "Ads", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Id)
   
        <button class="btn btn-primary btn-success" type="submit">Apply</button>
    }
</div>
